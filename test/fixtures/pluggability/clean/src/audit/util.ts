// FIXTURE (not compiled). A non-vendor core module — the clean fixture imports this, so the
// no-vendor-in-core rule must NOT fire. Do NOT import from real code.
export const auditUtil = "audit-util";
