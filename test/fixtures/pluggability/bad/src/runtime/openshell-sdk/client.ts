// FIXTURE (not compiled). A vendor adapter whose path uses the conventional `-sdk` suffix
// (openshell-sdk) — the boundary-widening must still flag a core import of it. Do NOT import real code.
export const vendorSdkClient = "openshell-sdk-client";
